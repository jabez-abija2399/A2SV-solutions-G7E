class Solution:
    def firstUniqChar(self, s: str) -> int:
       mapping = {}
       que = deque()
       
       for char in s:
        mapping[char] = mapping.get(char, 0) + 1

       for i, char in enumerate(s):
        if mapping[char] == 1:
            return i
       return -1   
            
