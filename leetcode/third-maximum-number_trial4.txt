class Solution:
    def thirdMax(self, nums: List[int]) -> int:
        nums =list(set(nums))
        
        nums.reverse()
        n = len(nums)
        nums.sort(reverse=True)
        
        if n >= 3:
            return nums[2]
        else:
            return nums[0]


